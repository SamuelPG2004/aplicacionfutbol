/*
  config.sample.js
  Copia este archivo a `config.js` y ajusta `window.API_BASE` antes de desplegar en GitHub Pages.
  Por ejemplo:
    window.API_BASE = 'https://mi-backend-ejemplo.com';

  Si trabajas en localhost durante el desarrollo no necesitas crear `config.js`.
*/

window.API_BASE = 'http://localhost:3000';
